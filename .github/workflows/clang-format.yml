name: Clang Format Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]  

jobs:
  clang-format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format

      - name: Check formatting
        run: |
          find . -path "./writing-a-c-compiler-tests" -prune -o -regex '.*\.\(cpp\|cc\|c\|h\|hpp\|hh\)' -print0 | xargs -0 clang-format --dry-run --Werror
